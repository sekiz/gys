# UzmanGYS Platform - Backend Environment Variables

# Server
NODE_ENV=development
PORT=5000

# Database (Local PostgreSQL)
# Connection Pool Ayarları (Sınırsız kullanıcı desteği için optimize edilmiş)
# connection_limit: Maksimum connection sayısı (PostgreSQL max_connections'in %80'i - yüksek limit)
# pool_timeout: Connection timeout süresi (saniye)
# Not: PostgreSQL max_connections değerini en az 200-300 yapmanız önerilir
DATABASE_URL=postgresql://postgres:postgres123@localhost:5432/uzmangys?schema=public&connection_limit=200&pool_timeout=30

# JWT Secrets (MUTLAKA DEĞİŞTİRİN!)
JWT_ACCESS_SECRET=your-super-secret-access-key-change-in-production-min-32-chars
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production-min-32-chars
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars # Backward compatibility

# Token Expiry (opsiyonel)
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d
RESET_TOKEN_EXPIRY=1h

# Security
BCRYPT_SALT_ROUNDS=10

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Şifre Sıfırlama URL (frontend URL'i)
RESET_PASSWORD_URL=http://localhost:3000
